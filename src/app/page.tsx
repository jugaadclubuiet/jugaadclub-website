import React from "react";
import { HeroSection } from "@/components/ComponentExporter";

type Props = {};

const Home = (props: Props) => {
  return (
    <>
      <HeroSection/>
    </>
  );
};

export default Home;
